<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Material">
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="android:colorPrimary">#4285f4</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">#3367d6</item>
        <item name="android:titleTextAppearance">@style/TitleStyle</item>
    </style>

    <style name="TitleStyle" parent="android:TextAppearance.Material.Title">
        <item name="android:paddingLeft">5dp</item>
    </style>

</resources>
